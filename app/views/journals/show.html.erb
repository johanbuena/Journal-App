<section class="container mb-20 mx-auto">
  <div class="container flex justify-between border-b p-1 m-1">
    <strong class="text-3xl"><%= @journal.journal_name %></strong>
    <a href="<%= new_journal_task_path(@journal, @task)%>" class="text-2xl">
      <i class="fas fa-plus text-base"></i>Task
    </a>
  </div>
  
  <%= render 'tasks/tasks' %>
</section>